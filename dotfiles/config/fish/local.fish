# Editable Fish overrides sourced by Home Manager's generated config.fish.
#
# Keep interactive-only experiments inside this guard. This file is an
# out-of-store symlink, so changes take effect in a new Fish shell without a
# Home Manager activation.
#
# Example:
# set -gx FISH_GREETING ""
